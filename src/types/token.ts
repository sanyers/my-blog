export type TokenItem = {
  userName: string
  token: string
  lastTime: number
}

export type CommentItem = {
  id: string
  userName: string
  blogId: string
  ctime: number
  comment: string
}
